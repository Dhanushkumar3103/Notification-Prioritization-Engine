# API Design

## 1. POST /notify
Accepts a notification event.
Returns decision and reason.

## 2. GET /decision/{notification_id}
Returns decision and explanation.

## 3. POST /rules
Updates prioritization rules dynamically.

## 4. GET /user-history/{user_id}
Returns user notification frequency metrics.

## 5. GET /health
Returns system health status.
